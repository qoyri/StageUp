<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/mobile_navigation"
            app:startDestination="@+id/nav_home">

    <fragment
            android:id="@+id/nav_home"
            android:name="com.example.stageup.ui.home.HomeFragment"
            android:label="@string/menu_home"
            tools:layout="@layout/fragment_home" />

    <fragment
            android:id="@+id/nav_message"
            android:name="com.example.stageup.ui.message.MessageFragment"
            android:label="@string/menu_message"
            tools:layout="@layout/fragment_message" />
    <!-- Removed nav_gallery -->

    <!-- Removed nav_slideshow -->

    <fragment
            android:id="@+id/nav_entreprise"
            android:name="com.example.stageup.ui.home.EntrepriseFragment"
            android:label="Entreprise"
            tools:layout="@layout/fragment_entreprise" />
</navigation>